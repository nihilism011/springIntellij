<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mySpringServer.myPage.mapper.PayMapper">
    <select id="payList" parameterType="hashmap" resultType="com.mySpringServer.myPage.model.Pay">
        SELECT *
        FROM payment
    </select>


</mapper>